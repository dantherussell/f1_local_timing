<div id="track_times" data-time-toggle-target="trackTimes">
  <% @track_events.each do |day, events| %>
    <h4><%= day %></h4>
    <table>
      <thead>
        <tr>
          <th>Series</th>
          <th>Session</th>
          <th>Start Time</th>
          <% if cookies[:authed] || Rails.env.development? %>
            <th></th>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <% events.each do |event| %>
          <tr <%= "class=f1".html_safe if event.series_name == "Formula 1" %>>
            <td><%= event.series_name %></td>
            <td><%= event.session_name %></td>
            <td><%= event.circuit_time %></td>
            <% if cookies[:authed] || Rails.env.development? %>
              <td><%= link_to "Edit", edit_season_weekend_event_path(@season, @weekend, event) %> | <%= link_to "Delete", season_weekend_event_path(@season, @weekend, event), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this event?", turbo_prefetch: false } %></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>
</div>
